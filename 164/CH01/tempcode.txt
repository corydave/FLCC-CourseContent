<html>
    
    <head>
        <title>Moar IO</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>

    <body>
        <center>
            <h1>Welcome to the Time Warp</h1>
        </center>

        Enter name: <input type="text" id="name"><br /><br />
        Enter age: <input type="text" id="age">&nbsp;&nbsp;
        <button onclick="futureAge()">Submit</button>

        <div id="result"></div>

        <!--
            We want the user to input their name and their age, then display
            on the screen how old they will be in two years.
        -->

        <script>

            // This function will get the name and the age of the user
            // and then output a message about how old they will be
            // in two years.
            function futureAge() {
                
                // let userName = document.querySelector("#name").value;
                let userName = document.getElementById('name').value;
                let userAge = Number(document.getElementById('age').value);
                console.log(userName, ' ', userAge);

                console.log('Welcome ' + userName + ', in two years you will be ' + (userAge + 2) + ' years.');
                // document.write('Welcome ' + userName + ', in two years you will be ' + (userAge + 2) + ' years.');
                
                document.getElementById('result').innerHTML = '<h1>Welcome</h1> ' + userName + ', in two years you will be ' + (userAge + 2) + ' years.';

            }

        </script>

    </body>

</html>





<html>
    <head>
        <title>Dave's World!</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <style>
            h1 {
                background-color: #1e90ff;
                color: #fff;
                padding: 10px 30px 10px 30px;
            }
        </style>
    </head>

    <body>
        <h1>Hello world!</h1>

        <!--
              script tag goes right before closing the body
        -->
        <script>
            'use strict';
            /*
                This is BLOCK COMMENTING
                REASON 1 - To leave notes for future me
                           and also whomever works on this code later

                REASON 2 - TODO lists
            */

            let favoriteTVShow = 'Community';
            window.alert(favoriteTVShow);

            // window.alert('hi');
            // window.alert("hello");
            // window.alert(3);
            // window.confirm('hiya');

            let close = window.confirm('Would you like to close this window?');
            console.log(close);
            document.write('you choose ' + close);

            if (close == true) {
                window.close();
            } else {
                window.alert('OK!!!!');
            }

            let name = window.prompt('Enter your name:');
            document.write('Hello ' + name);

            console.log(window.innerHeight);
            console.log(window.innerWidth);

            // window.close();

            console.log('why hello there!');

            document.write('<h1>again</h1>, salutations.');


        </script>

    </body>
</html>




var age = 44;
let name = 'dave';
